# bnasec: physical console autologins as bna; .zprofile then starts Hyprland
# on tty1 only — other ttys keep a normal login shell prompt.
[Service]
ExecStart=
ExecStart=-/sbin/agetty -o '-p -- \u' --noclear --autologin bna %I $TERM
